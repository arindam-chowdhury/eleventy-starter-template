<nav class="main-navbar">
    <div class="navbar-logo">
        <a href="/">
            <img src="{{ site.logo.url }}" alt="{{ site.logo.alt }}" class="logo-image" onerror="this.onerror=null;this.src='https://placehold.co/40x40/E8E8E8/333333?text=Logo';" />
        </a>
    </div>
    {# <ul class="navbar-links">
        {% for item in site.navigation %}
            <li class="navbar-item">
                <a href="{{ item.url }}" class="navbar-link">{{ item.text }}</a>
            </li>
        {% endfor %}
    </ul> #}
    <div class="navbar-actions">
        <a href="/careers/" class="btn btn-careers">Careers</a>
    </div>
</nav>

